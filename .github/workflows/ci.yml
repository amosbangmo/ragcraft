name: CI

on:
  push:
  pull_request:

jobs:
  lint-test-boot:
    runs-on: ubuntu-latest

    env:
      OPENAI_API_KEY: ci-test-key

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install ruff

      - name: Lint (critical rules)
        run: ruff check . --select E9,F63,F7,F82

      - name: Run backend runtime tests
        run: python -m unittest discover -s tests/backend -p "test_*.py"

      - name: Run integration tests
        run: python -m unittest discover -s tests/integration -p "test_*.py"

      - name: Run quality gates
        run: python -m unittest discover -s tests/quality -p "test_*.py"

      - name: Basic app boot check
        run: |
          python -c "from src.app.ragcraft_app import RAGCraftApp; RAGCraftApp(); print('boot-ok')"
