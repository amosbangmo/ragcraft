[project]
name = "ragcraft-api"
version = "0.1.0"
description = "RAGCraft FastAPI backend (src layout)"
requires-python = ">=3.11"
readme = "README.md"

[tool.setuptools.packages.find]
where = ["src"]
