# RAGCraft frontend (Streamlit)

Source root: `frontend/src/` (pages, components, services, utils).

- Run from `frontend/`: `streamlit run app.py` with `PYTHONPATH` including `frontend/src` (see repo `pyproject.toml`).
